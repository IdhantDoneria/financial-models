# Financial Models — pinned dependencies (Python 3.10+)
#
# This is the CORE tier: everything needed to import and run the twelve
# models (`from src import ...`), and the true minimum api/premium.py
# needs on Vercel. Kept as small as possible on purpose — Python functions
# have a 500MB uncompressed bundle limit, and pandas+scipy+numpy alone are
# already a meaningful fraction of that on Linux.
#
# Testing needs one more file (pytest + the two libraries the pipeline
# tests exercise): pip install -r requirements.txt -r requirements-test.txt
# The interactive notebook and full local PDF/export workflow need a third:
# requirements-notebook.txt. See the README's Installation section.
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
requests==2.34.2
