# Extras the test suite needs beyond requirements.txt's numeric core:
# pytest itself, plotly (tests/test_models.py calls .visualize() on every
# model), and the two libraries tests/pipeline/test_pipeline.py exercises
# directly (a reportlab-built synthetic PDF, read back by pypdf; an xlsx
# export via openpyxl).
#
# Install in ADDITION to requirements.txt:
#   pip install -r requirements.txt -r requirements-test.txt
#   pytest tests/ -q
#
# Kept separate from requirements.txt so Vercel's Python function build
# (api/premium.py) doesn't bundle test-only dependencies — see that file's
# header comment.
pytest==9.1.1
plotly==6.8.0
pypdf==6.14.2
reportlab==5.0.0
openpyxl==3.1.5
